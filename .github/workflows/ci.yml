name: CI

on:
  push:
    branches:
      - main
      - ops-*
  pull_request:

jobs:
  test:
    name: aiops tests
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests
        run: scripts/test.sh
